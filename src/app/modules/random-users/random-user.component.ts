import { Injectable } from '@angular/core';

/**
 * Servicio auxiliar de usuarios aleatorios.
 *
 * @remarks
 * Servicio base registrado de forma global.
 * El servicio principal de consumo de API es `RandomUsersService`.
 */
@Injectable({
  providedIn: 'root',
})
export class RandomUsers {
  
}